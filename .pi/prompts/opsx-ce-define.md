# Define OpenSpec Intent

Use CLI artifactPaths, resolvedOutputPath, and changeRoot as layout authority. Read concrete dependency paths, never guessed files. Verify non-empty owned output and refreshed existingOutputPaths before completion. Missing metadata blocks work.

Resolve an explicit change or an unambiguous existing change from context or `openspec list --json`; otherwise stop for selection. Never create a change. Run `openspec status --change "<change>" --json`; reject any schemaName other than `compound-intent-driven`. Honor planningHome, changeRoot, and actionContext. For a named store, discover its id with `openspec store list --json` and retain `--store <id>` on change commands.

Run `openspec instructions proposal --change "<change>" --json`. Read its template and every concrete dependency path or contextFiles entry, plus existing proposal and relevant repository learnings. Build a packet containing change, schemaName, artifactId, instruction, resolvedOutputPath, settled decisions, and allowed mutation paths. Missing paths or conflicting evidence are blockers; never guess ownership.

This invocation authorizes a definition envelope: proposal, then concrete specs, stopping before design. Require each artifact to be ready when reached; an already completed artifact needs an explicit owning-artifact revision decision. Preserve settled artifacts instead of re-asking plan/work scope.

Ground the problem in repository evidence and relevant learnings first. Research only material evidence gaps; use read-only sources and record sources and uncertainty. Use ideation when competing directions need comparison, then ce-brainstorm reasoning to resolve intent, alternatives, boundaries, capabilities, and observable success. Skip discovery already settled by artifacts. Interview only for unresolved material decisions, one question at a time; wait for each answer. An unanswered question blocks writing dependent claims. Keep findings and decisions in owned OpenSpec artifacts, not separate research or brainstorm files.

Write proposal only to its instruction-resolved output within changeRoot. Refresh status and verify non-empty contents and existingOutputPaths. Then run `openspec instructions specs --change "<change>" --json`, read its template and concrete dependencies, and extend allowed mutation paths only from that instruction output. Use artifactPaths, resolvedOutputPath, and generates relative to changeRoot to resolve non-empty `<changeRoot>/specs/<capability>/spec.md` files for approved capabilities; never write a literal `specs/**/*.md` glob or count an empty directory as completion. Missing concrete output resolution blocks writing. Use OpenSpec deltas, SHALL/MUST requirements and exact `#### Scenario:` headings with GIVEN/WHEN/THEN; copy complete prior requirement blocks before modifying them. Verify every capability file and refreshed existingOutputPaths/status. Do not write design, implement code, or create learnings.

OpenSpec overrides conflicting CE skill instructions. No parallel CE plans/trackers, commits, branches, pushes, issues, PRs, automatic stage selection/advance outside the authorized proposal-to-concrete-specs transition inside this definition envelope, or archive. No adapter-managed worktrees or external side effects. Do not run CE-native side effects. Stop after this definition envelope, before design.

Refresh `openspec status --change "<change>" --json` even on a blocked handoff when possible. Stop on blockers, no-op, or stale state; never retry unchanged inputs. Return outcome, proof, mutations, blocker, and refreshed status in this universal result packet: Outcome (completed/blocked/no-op/stale); Change/Schema/Artifact-or-task/Batch/Worker/Dependency layer (explicit values, none if inapplicable); Proof (sources, gate checks, observed results, not invented tests); Mutations (exact paths); OpenSpec state (refreshed status or unavailable); Continuation (remaining owned scope and prerequisites); Blocker (or none); Next command (one exact command, not executed). On successful definition return `/opsx-ce-plan "<change>" design`; otherwise return the exact safe status/instructions command for inspection, or `none` with the required decision if no command is safe. Substitute resolved values, retain named-store selection, and never infer stage readiness from this recommendation. Report unavailable CLI honestly; never install a runtime.
